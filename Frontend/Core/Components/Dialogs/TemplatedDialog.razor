@if (!IsHidden)
{
	<div class="modal-backdrop" @onclick="Close">
		<div class="modal-content" @onclick:stopPropagation="true">
			@if (ChildContent is not null)
			{
					@ChildContent
			}
			<button class="btn btn-outline-danger w-100 mt-3" @onclick="Close">Zamknij</button>
		</div>
	</div>
}
